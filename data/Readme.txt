Here's the first ~100 audio clips from the site. there's still like ~450 left, but those could go whenever.
These would probably more useful in .wav format, since that stores the values of the samples as distinct values, and it would be a lot easier to get data off that than the compressed .mp3 form.  eeh

